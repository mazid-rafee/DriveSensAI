# Start the crime prediction server (port 8000)
conda activate dmd
cd /Users/zaimazarnaz/Desktop/IOSApps/DriveSensAI/AIPackages/CrimePredictor
python -m uvicorn api.app:app --host 0.0.0.0 --port 8000

# Start the drowsiness detection server (port 8001) — use a second terminal
conda activate dmd
cd /Users/zaimazarnaz/Desktop/IOSApps/DriveSensAI/AIPackages/DrowsinessDetection
python -m uvicorn api.app:app --host 0.0.0.0 --port 8001
